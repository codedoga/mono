{
  "name": "{{LIB_NAME}}",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "lint": "bunx biome check src/"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
