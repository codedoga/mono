{
  "name": "{{APP_NAME}}",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "lint": "bunx biome check src/"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
