# Bun-Konfiguration für test-app
# Docs: https://bun.sh/docs/runtime/bunfig

[install]
# Peer-Dependencies automatisch installieren
peer = true

[install.scopes]
# Erlaubt Imports aus dem libs Verzeichnis

[resolve.paths]
"@libs/*" = ["../../libs/*"]
